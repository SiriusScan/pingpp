{
  "name": "ping++",
  "install": "go mod download && go build ./... && go vet ./... && (command -v golangci-lint >/dev/null 2>&1 || curl -sSfL https://raw.githubusercontent.com/golangci/golangci-lint/HEAD/install.sh | sudo sh -s -- -b /usr/local/bin latest)"
}
